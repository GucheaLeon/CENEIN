window.APP_CONFIG = {
  // Dejar API_URL vacía "" para que use la URL relativa (/api) y funcione con el proxy o servidor local.
  // Si deseas forzar un puerto o host específico, puedes cambiarlo aquí (ej: "http://localhost:4000").
  API_URL: "",
};
